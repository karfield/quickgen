package main

import "testing"

func TestConfigParse(t *testing.T) {
	/*
		config, err := ParseFromFile("/Users/karfield/.codegen-automators/maven-webapp/GEN.toml")
		if err != nil {
			t.Errorf("parse error %v", err)
		} else {
			t.Logf("config content: %v", config)
		}*/
}
